.DS_Store
.claude/

# Stray zips stay out of version control, but the archive's download files
# are content, not build output - the Scratch schemes of work and FOB Games
# bundles linked from the legacy posts exist nowhere else.
*.zip
!assets/**/*.zip
